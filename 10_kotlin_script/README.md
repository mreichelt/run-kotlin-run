# Kotlin Script `.kts`

Simply right-click on [primes.kts](primes.kts) and run it!
Or in the shell:

```bash
kotlin primes.kts
```

Needs kotlin CLI tools installed with: `brew install kotlin`

## Bonus

Executable works, too!
